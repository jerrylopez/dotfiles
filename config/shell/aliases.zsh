# |----------------------------------------------------------------
# | General Aliases
# |----------------------------------------------------------------

alias cls='clear'
alias reload='. ~/.zshrc'

alias dotfiles="cd $DOTFILES"
alias hosts="sudo vi /etc/hosts"

# |----------------------------------------------------------------
# | Git Aliases
# |----------------------------------------------------------------

alias nah='git reset --hard; git clean -df'